name: Validate

on:
  pull_request:
    branches: [main]
  # Also run on direct pushes to the audit branch so the build/test status is
  # visible without opening a PR. Remove once the branch is merged.
  push:
    branches: [audit/technical-fixes]

# Cancel in-progress runs for the same branch/PR
concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Sync SvelteKit
        run: npx svelte-kit sync

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run check

      - name: Test
        run: npm run test

      # npm audit: fail the build on production high/critical vulnerabilities.
      # Dev-only advisories are noise on this branch (mostly typescript-eslint,
      # vitest plugins, ...) and we don't ship them, so they are excluded via
      # --omit=dev rather than silenced. This step is gating: a high/critical
      # CVE in a shipped dependency must block the PR.
      - name: npm audit (production deps)
        run: npm audit --omit=dev --audit-level=high

  backend-fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Rust
        # dtolnay/rust-toolchain has no tagged releases — pin to the current
        # commit on `stable`. Bump deliberately when refreshing the toolchain.
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable @ 2026-05-08
        with:
          components: rustfmt

      - name: Format check
        working-directory: src-tauri
        run: cargo fmt --check

  backend-clippy:
    needs: backend-fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Rust Backend
        uses: ./.github/actions/setup-rust-backend
        with:
          rust-components: clippy
          cache-key: clippy

      - name: Clippy
        working-directory: src-tauri
        run: cargo clippy --all-targets -- -D warnings

  backend-test:
    needs: backend-fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Rust Backend
        uses: ./.github/actions/setup-rust-backend
        with:
          rust-components: ''
          cache-key: test

      - name: Test
        working-directory: src-tauri
        run: cargo test

  backend-audit:
    needs: backend-fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Setup Rust Backend
        uses: ./.github/actions/setup-rust-backend
        with:
          rust-components: ''
          cache-key: audit
      # cargo-audit prints advisories from RustSec/advisory-db.
      # Informational for now: it runs but does not fail the build, so
      # transient advisories on transitive deps don't block PRs. Promote to
      # failing once the advisory baseline is clean.
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Run cargo-audit
        working-directory: src-tauri
        run: cargo audit
        continue-on-error: true

  backend:
    needs: [backend-fmt, backend-clippy, backend-test, backend-audit]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check backend results
        # backend-audit is intentionally not gating: it runs `continue-on-error: true`
        # so the aggregator only fails when fmt/clippy/test fail. The audit
        # output is visible in the run logs as advisory information.
        run: |
          if [ "${{ needs.backend-fmt.result }}" != "success" ] || \
             [ "${{ needs.backend-clippy.result }}" != "success" ] || \
             [ "${{ needs.backend-test.result }}" != "success" ]; then
            echo "One or more backend jobs failed"
            exit 1
          fi
